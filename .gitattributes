# Normalize to LF in the repo; Windows editors can still handle LF.
* text=auto eol=lf

# (Optional) if you keep Windows batch files, force CRLF for those:
*.bat text eol=crlf

# Ensure .env would be LF if it ever gets committed (still keep it ignored)
.env text eol=lf
